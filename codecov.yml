codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  round: down
  range: 50..90
  status:
    project:
      default:
        target: auto
        threshold: 1%
      discovery:
        flags:
          - discovery

flags:
  discovery:
    paths:
      - src
